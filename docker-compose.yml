version: '3.9'

services:
  postgres:
    image: postgres:latest
    container_name: cinecore-postgres
    environment:
      POSTGRES_DB: CineCore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    restart: always